# Configuration file generated by Posit Publisher.
# Please review and modify as needed. See the documentation for more options:
# https://github.com/posit-dev/publisher/blob/main/docs/configuration.md
"$schema" = "https://cdn.posit.co/publisher/schemas/posit-publishing-schema-v3.json"
type = "r-shiny"
entrypoint = "app.R"
title = "technique checklist"
files = [
    "/app.R",
    "/renv.lock",
    "/.posit/publish/technique checklist-4L63.toml",
    "/.posit/publish/deployments/deployment-06A1.toml",
]
validate = true
# Declared so a publish never drops the content's env var (only the NAME
# lives here; the value is set in the Connect Cloud dashboard).
secrets = ["CCBC_DB_PWD"]
product_type = "connect_cloud"

[r]
